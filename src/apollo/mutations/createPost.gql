import gql from 'graphql-tag';

export default gql`
  mutation($groupId: ID!, $content: String!) {
    createPost(
      data: {
        group: {
          connect: { 
            id: $groupId 
          } 
        }
        content: $content
      }
    ) {
      id
    }
  }
`